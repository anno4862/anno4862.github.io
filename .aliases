source .env
alias update_hugo='echo "HUGO_VERSION=$(curl -s https://formulae.brew.sh/api/formula/hugo.json | jq -r '.versions.stable')" > .env'
alias hugo="docker run --rm -p 1313:1313 -v \$(pwd):/src docker.io/hugomods/hugo:debian-ci-non-root-${HUGO_VERSION}"
